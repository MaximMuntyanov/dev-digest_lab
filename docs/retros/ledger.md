# SDD Workflow Retro Ledger

One row per analyzed multi-agent SDD run (appended by the `workflow-retro` skill),
so runs can be compared over time. Deep-mode numbers include nested sub-agents.

| date | feature | tokens_in | tokens_out | cache_read | cache_hit | tool_calls | duration | agents | top_recommendation |
|------|---------|-----------|------------|------------|-----------|------------|----------|--------|--------------------|
